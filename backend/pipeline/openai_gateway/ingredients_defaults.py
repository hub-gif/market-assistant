"""详情页配料多模态识别：提示词、温度与 Referer 等（与 `AI_crawler` 顶部运行配置同口径）。"""
from __future__ import annotations

# 与 AI_crawler 中 PROMPT_DEFAULT 保持同步（脚本入口仍可在本地覆盖）
IMAGE_REFERER = "https://www.jd.com/"

USER_PROMPT = ""
PROMPT_DEFAULT = (
    "请识别图片中的原料/成分信息，只输出原料或成分名称列表本身，用中文逗号或顿号分隔为连续一段，不要多行换行。"
    "适用范围包括：（1）食品包装「配料表」；（2）化妆品、护肤品、洗护等详情长图中的「成分/全成分/功效成分/核心成分」"
    "或左侧/底部逐条列出的原料名（如透明质酸钠、××提取物、××酶等）。不要将菜品做法、步骤、烹饪备料误当成配料表。"
    "每种名称只出现一次，禁止重复罗列同一添加剂；食品图中多行同类添加剂可概括为「食品添加剂（按国家标准使用）」。"
    "【禁止猜测】必须严格依据图中清晰可见的印刷文字归纳；不得根据品类、常识或模糊字迹推测、补全、编造。"
    "若图中既无可辨认的配料表，也无任何可逐字确认的原料/成分名词（仅有纯广告话术、卖点标语、无具体原料名），"
    "则只输出且仅输出一句「无法识别图片中的配料或成分」（不要解释、不要道歉长文、不要列出疑似项）。"
)
TEMPERATURE = 0.0
MAX_TOKENS = 2048
# 部分 Qwen 网关需要关闭 thinking
QWEN_OMNI_TEMPLATE = False
